import React, { Fragment } from 'react';

const NavBar = () => {
    return (
        <nav className="navbar">
            <h1>
                icon
        </h1>
            <Fragment>
                info
        </Fragment>
        </nav>
    )
}

export default NavBar
